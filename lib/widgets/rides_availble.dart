import 'package:flutter/material.dart';

class RidesAvailable extends StatefulWidget {
  const RidesAvailable({Key? key}) : super(key: key);

  @override
  State<RidesAvailable> createState() => _RidesAvailableState();
}

class _RidesAvailableState extends State<RidesAvailable> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [],
      ),
    );
  }
}
